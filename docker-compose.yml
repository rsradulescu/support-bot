version: '3.8'
services:
  bot:
    build: .
    volumes:
      - ./faiss_index:/app/faiss_index      # persist index
      - ./data/wiki:/data/wiki              # wiki source
    ports:
      - '8000:8000'
    env_file:
      - .env